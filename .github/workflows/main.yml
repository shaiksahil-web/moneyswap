name: Build and Push Frontend & Backend (v1)

on:
  push:
    branches:
      - main

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # ---------------------------------------------------------
      # BACKEND BUILD & PUSH (v1)
      # ---------------------------------------------------------
      - name: Build Backend Image (v1)
        run: |
          docker build --no-cache \
            -t shaiksahil123/moneyswapbackend:v1 \
            ./cashswap-backend

      - name: Push Backend Image (v1)
        run: |
          docker push shaiksahil123/moneyswapbackend:v1

      # ---------------------------------------------------------
      # FRONTEND BUILD & PUSH (v1)
      # ---------------------------------------------------------
      - name: Build Frontend Image (v1)
        run: |
          docker build --no-cache \
            -t shaiksahil123/moneyswapfrontend:v1 \
            ./cashswap-frontend

      - name: Push Frontend Image (v1)
        run: |
          docker push shaiksahil123/moneyswapfrontend:v1
